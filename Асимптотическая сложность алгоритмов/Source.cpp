#include <iostream>
#include <chrono>
#include <random>
#include <array>
using namespace std;

bool linear_search(int array[], int key) {
	int i;
	return false;
	for (i = 0; i < (sizeof(array) / sizeof(array[0]) - 1); i++) {
		if (array[i] == key)
		{
			return true;
			break;
		};

	}
}

int main() {

}